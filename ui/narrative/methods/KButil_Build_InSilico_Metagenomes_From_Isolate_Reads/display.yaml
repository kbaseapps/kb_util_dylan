#
# define display information
#
name: Build InSilico Metagenomes from Isolate Reads
tooltip: |
    Create reads libraries for synthetic metagenomes from isolate reads libraries following user-defined proportions
screenshots: []

icon: kb-pink.png

#
# define a set of similar methods that might be useful to the user
#
suggestions:
    apps:
        related:
            []
        next:
            []
    methods:
        related:
            []
        next:
            []

#
# Configure the display and description of parameters
#
parameters :
    desc:
        ui-name : |
            Description
        short-hint : |
            Describe Purpose or Contents of Output ReadsLibrary
        placeholder: |
            Enter Description
    input_refs:
        ui-name : |
            Input reads libraries
        short-hint : |
            Reads Lib Names here (SingleEndLibrary or PairedEndLibrary, but not both)
    output_name:
        ui-name : |
            Output reads lib name
        short-hint : |
            Name for the Output Reads Library

description : |
    <p>This method allows the user to take fractions of isolate reads files and combine them into <i>in silico</i> metagenomesmerge following user-defined proportions.</p>
